{{ form_start(form) }}
<div>
    {{ form_errors(form) }}
</div>
<div>
    {{ form_label(form.gridname, 'Name your grid : ') }}
    {{ form_widget(form.gridname, {'attr': {'class': 'forminput'}}) }}
</div>

<button class="btunconnection">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
