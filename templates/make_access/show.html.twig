{% extends 'base.html.twig' %}

{% block title %}MakeAccess{% endblock %}

{% block body %}
    <h1>MakeAccess</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ make_access.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_make_access_index') }}">back to list</a>

    <a href="{{ path('app_make_access_edit', {'id': make_access.id}) }}">edit</a>

    {{ include('make_access/_delete_form.html.twig') }}
{% endblock %}
