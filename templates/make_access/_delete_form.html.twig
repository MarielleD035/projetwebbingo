<form method="post" action="{{ path('app_make_access_delete', {'id': make_access.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ make_access.id) }}">
    <button class="btn">Delete</button>
</form>
